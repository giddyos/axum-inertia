[package]
name = "inertia-macros-actix-only"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
inertia-actix = { path = "../../../../inertia-actix", default-features = false, features = ["macros"] }

[workspace]
